name: hooks
type: leaf
version: 0.1.0
description: 표준 5-hook plugin — Claude Code 이벤트와 1:1 매핑 (session_start / user_prompt_submit / pre_tool_use / post_tool_use / stop) + post_commit (git)

provides:
  - session_start
  - user_prompt_submit
  - pre_tool_use
  - post_tool_use
  - stop
  - post_commit

requires_adapter: true   # provider 어댑터가 필요
